version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 20
  build:
    commands:
      - echo "Build listo; no zipeamos manualmente"
artifacts:
  files:
    - index.js
    - node_modules/**/*   # si no tenés deps, podés quitar esta línea
  discard-paths: yes
  name: function.zip